version: "3.9"

services:
  nexus:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./memory:/app/memory        # persist task data
      - ./.env:/app/.env            # inject API keys
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
